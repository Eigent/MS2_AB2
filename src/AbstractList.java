abstract class AbstractList implements IList {
    //Abstrakte Klasse mit der abstrakten Methode get, die im vererbten Objekt (ArrayList) ausdefiniert wird.
    //Implements das Interface IList
    @Override
    public abstract void get();

    @Override
    public void equals() {

    }
}
